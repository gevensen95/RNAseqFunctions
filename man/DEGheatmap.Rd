% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DEGheatmap.R
\name{DEGheatmap}
\alias{DEGheatmap}
\title{Heatmap of top differentially expressed genes}
\usage{
DEGheatmap(
  counts,
  metadata,
  results,
  method = c("DESeq2", "edgeR"),
  n = 30,
  annotate_by = NULL,
  geneID = "Gene"
)
}
\arguments{
\item{counts}{Matrix or data.frame of raw counts, genes as rows, samples
as columns (same orientation as \code{\link[=makeComp]{makeComp()}}).}

\item{metadata}{Data.frame of sample metadata. \code{rownames(metadata)} must
match \code{colnames(counts)} exactly, in the same order.}

\item{results}{DEG results data.frame (e.g. from \code{\link[=makeComp]{makeComp()}}) used to
pick and order the top genes by padj.}

\item{method}{Which engine to use for normalization: \code{"DESeq2"}
(default, via \code{vst()}) or \code{"edgeR"} (via \code{cpm(log = TRUE)}).}

\item{n}{Number of top genes (by padj) to include.}

\item{annotate_by}{Optional metadata column name to group and label
samples by (columns are reordered and split into facets by this
variable).}

\item{geneID}{Column in \code{results} containing gene identifiers matching
\code{rownames(counts)}.}
}
\value{
A ggplot2 heatmap object.
}
\description{
Normalizes raw counts, picks the top \code{n} genes by padj from a DEG results
data.frame (e.g. from \code{\link[=makeComp]{makeComp()}}), and plots a z-scored expression
heatmap across samples.
}
