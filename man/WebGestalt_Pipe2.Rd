% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WebGestalt_Pipe2.R
\name{WebGestalt_Pipe2}
\alias{WebGestalt_Pipe2}
\title{WebGestalt Enrichment}
\usage{
WebGestalt_Pipe2(
  mode = "ORA",
  results,
  alpha = 0.05,
  FC = 0.5,
  DB = NULL,
  species = "mmusculus",
  projectName = NULL,
  geneID = "Gene",
  outputTerms = 100,
  minEnrichmentRatio = 1
)
}
\arguments{
\item{mode}{"ORA" or "GSEA". Note: GSEA mode runs GSEA on the log2FC values.}

\item{results}{A \code{\link[=makeComp]{makeComp()}} output data.frame. Any data frame with a
gene identifier column, "padj", and "log2FoldChange" also works — genes
are looked up by name via \code{geneID}, not by column position. Rows with NA
padj or log2FoldChange are dropped before running.}

\item{alpha}{pAdj cut-off for DEGs (ORA only)}

\item{FC}{absolute log2FC cut-off for DEGs (ORA only)}

\item{DB}{WebGestalt enrichDatabase to query against, see WebGestaltR::listGeneSet() for a full list.}

\item{species}{WebGestalt organism, see WebGestaltR::listOrganism() for a full list.}

\item{projectName}{Name of raw data folder. If \code{NULL} (the default), WebGestaltR runs without writing output files.}

\item{geneID}{Column for genes to use for enrichment}

\item{outputTerms}{The threshold for the top method. The default is 100.}

\item{minEnrichmentRatio}{Absolute enrichmentRatio cut-off applied after
combining up/down ORA results (ORA only). Terms with weaker enrichment
than this are dropped. Default 1 (no enrichment signal).}
}
\value{
A dataframe of enriched terms (ORA or GSEA) with an "ID" column
("geneSet: description") in column 1. Returns \code{NULL} (invisibly) with a
message if no terms are returned.
}
\description{
Runs GO/pathway enrichment (ORA or GSEA) on a DEG results data.frame
using WebGestaltR.
}
