% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PCAplot.R
\name{PCAplot}
\alias{PCAplot}
\title{Plots a sample PCA}
\usage{
PCAplot(
  counts,
  metadata,
  method = c("DESeq2", "edgeR"),
  color_by = NULL,
  shape_by = NULL,
  ntop = 500
)
}
\arguments{
\item{counts}{Matrix or data.frame of raw counts, genes as rows, samples
as columns (same orientation as \code{\link[=makeComp]{makeComp()}}).}

\item{metadata}{Data.frame of sample metadata. \code{rownames(metadata)} must
match \code{colnames(counts)} exactly, in the same order.}

\item{method}{Which engine to use for normalization: \code{"DESeq2"}
(default, via \code{vst()}) or \code{"edgeR"} (via \code{cpm(log = TRUE)}).}

\item{color_by}{Optional metadata column name to color points by.}

\item{shape_by}{Optional metadata column name to shape points by.}

\item{ntop}{Number of most-variable genes to use for the PCA.}
}
\value{
A ggplot2 PCA plot object.
}
\description{
Normalizes raw counts (via DESeq2's variance-stabilizing transform or
edgeR's log-CPM) and plots a PCA of samples using the top variable genes,
for quick QC of sample clustering and batch effects.
}
