% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeComp.R
\name{makeComp}
\alias{makeComp}
\title{Run a differential expression comparison}
\usage{
makeComp(
  counts,
  metadata,
  formula = NULL,
  contrast = NULL,
  method = c("DESeq2", "edgeR"),
  alpha = 0.05
)
}
\arguments{
\item{counts}{Matrix or data.frame of raw (un-normalized) integer counts,
genes as rows, samples as columns. Row names must be gene identifiers.}

\item{metadata}{Data.frame of sample metadata. \code{rownames(metadata)} must
match \code{colnames(counts)} exactly, in the same order.}

\item{formula}{Design formula, e.g. \code{~condition}. If \code{NULL} (the
default), a \code{~condition} design is used provided \code{metadata} has a column
named "condition"; otherwise an error is raised asking for an explicit
formula.}

\item{contrast}{The comparison to extract.
\itemize{
\item For \code{method = "DESeq2"}: a character vector of length 3, e.g.
\code{c("condition", "treated", "control")}, passed to
\code{\link[DESeq2:results]{DESeq2::results()}}. If \code{NULL}, DESeq2's default contrast (the last
level of the last formula variable vs. the reference level) is used.
\item For \code{method = "edgeR"}: a single contrast string suitable for
\code{\link[limma:makeContrasts]{limma::makeContrasts()}}, e.g. \code{"conditiontreated - conditioncontrol"},
evaluated against \code{model.matrix(formula, metadata)}. If \code{NULL}, the
last coefficient of the design matrix is tested.
}}

\item{method}{Which engine to use: \code{"DESeq2"} (default) or \code{"edgeR"}.}

\item{alpha}{Significance cut-off passed through to DESeq2's
\code{results(alpha = )} for independent filtering. Ignored for edgeR.}
}
\value{
A data.frame of DEG results with \code{log2FoldChange} as column 2
and gene IDs (as column "Gene") as column 7, ready to pass to
\code{\link[=eRupt2]{eRupt2()}} or \code{\link[=WebGestalt_Pipe2]{WebGestalt_Pipe2()}}.
}
\description{
Takes a raw counts matrix and sample metadata, runs differential
expression with either DESeq2 or edgeR, and returns a results
data.frame shaped for direct use with \code{\link[=eRupt2]{eRupt2()}} and
\code{\link[=WebGestalt_Pipe2]{WebGestalt_Pipe2()}}: \code{log2FoldChange} in column 2, gene IDs in column 7
(named "Gene"), and columns named "Gene", "padj", and "log2FoldChange".
}
\details{
DESeq2 and edgeR are not hard dependencies of RNAseqBasic (to keep
installs light) — install whichever engine you plan to use via
\code{BiocManager::install("DESeq2")} or \code{BiocManager::install("edgeR")}.
Using a \code{contrast} with \code{method = "edgeR"} additionally requires the
\code{limma} package.
}
