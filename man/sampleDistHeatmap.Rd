% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampleDistHeatmap.R
\name{sampleDistHeatmap}
\alias{sampleDistHeatmap}
\title{Sample-to-sample distance heatmap}
\usage{
sampleDistHeatmap(counts, metadata, method = c("DESeq2", "edgeR"))
}
\arguments{
\item{counts}{Matrix or data.frame of raw counts, genes as rows, samples
as columns (same orientation as \code{\link[=makeComp]{makeComp()}}).}

\item{metadata}{Data.frame of sample metadata. \code{rownames(metadata)} must
match \code{colnames(counts)} exactly, in the same order.}

\item{method}{Which engine to use for normalization: \code{"DESeq2"}
(default, via \code{vst()}) or \code{"edgeR"} (via \code{cpm(log = TRUE)}).}
}
\value{
A ggplot2 heatmap object.
}
\description{
Normalizes raw counts and plots a heatmap of Euclidean distances between
samples (hierarchically clustered), for QC of sample similarity and
outlier detection. A complement to \code{\link[=PCAplot]{PCAplot()}}.
}
